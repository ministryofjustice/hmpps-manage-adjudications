{% extends "../partials/layout.njk" %}
{% from "govuk/components/breadcrumbs/macro.njk" import govukBreadcrumbs %}
{% from "../partials/card.njk" import card %}

{% set title = "Adjudications" %}
{% set feedbackBannerBackground = '#f0f4f5 '%}

{% block pageTitle %}
  {{ title }} - Digital Prison Services
{% endblock %}

{% block main %}
  <div class="govuk-width-container">
    {{ govukBreadcrumbs({
      collapseOnMobile: true,
      items: [{
        text: "Digital Prison Services",
        href: digitalPrisonServiceUrl
      }],
      classes: "govuk-!-display-none-print"
    }) }}

    {% from "govuk/components/notification-banner/macro.njk" import govukNotificationBanner %}

    {% set html %}
      <p class="govuk-notification-banner__heading">
        The adjudications service will undergo maintenance on 30th April 2026. Please ensure you have completed necessary activities in the service before 17:00. Normal service is expected to resume by 19:00.
      </p>
    {% endset %}

    {{ govukNotificationBanner({
      html: html
    }) }}

    <h1 class="govuk-heading-l govuk-!-margin-top-7">{{ title }}</h1>
  </div>

  <div class="homepage-content">
        <div class="govuk-width-container">
    <div class="govuk-grid-row">
      <ul class="card-group">
        {% for task in reporterTasks %}
          <li class="govuk-grid-column-one-third card-group__item">
            {{ card({
                "href": task.href,
                "clickable": "true",
                "heading": task.heading,
                "description": task.description,
                "id": task.id
              }) }}
          </li>
        {% endfor %}
      </ul>
      {% if reporterTasks.length %}
        <hr class="govuk-section-break govuk-section-break--xl govuk-section-break--visible govuk-!-margin-bottom-7 govuk-!-margin-top-2" data-qa="section-break">
      {% endif %}
    </div>

      <div class="govuk-grid-row">
        <ul class="card-group govuk-!-margin-bottom-0">
          {% for task in reviewerTasks %}
            <li class="govuk-grid-column-one-third card-group__item">
              {{ card({
                "href": task.href,
                "clickable": "true",
                "heading": task.heading,
                "description": task.description,
                "links": task.links,
                "id": task.id
              }) }}
            </li>
          {% endfor %}
        </ul>
        {% if reviewerTasks.length %}
          <hr class="govuk-section-break govuk-section-break--xl govuk-section-break--visible govuk-!-margin-bottom-7 govuk-!-margin-top-2" data-qa="section-break">
        {% endif %}
      </div>

      <div class="govuk-grid-row">
        <ul class="card-group">
          {% for task in disRelatedTasks %}
            <li class="govuk-grid-column-one-third card-group__item">
              {{ card({
                  "href": task.href,
                  "clickable": "true",
                  "heading": task.heading,
                  "description": task.description,
                  "id": task.id
                }) }}
            </li>
          {% endfor %}
        </ul>
        {% if disRelatedTasks.length %}
          <hr class="govuk-section-break govuk-section-break--xl govuk-section-break--visible govuk-!-margin-bottom-7 govuk-!-margin-top-2" data-qa="section-break">
        {% endif %}
      </div>

      <div class="govuk-grid-row">
        <ul class="card-group">
          <li class="govuk-grid-column-one-third card-group__item">
            {% if dataInsightsTask.enabled %}
              {{ card({
                  "href": dataInsightsTask.href,
                  "clickable": "true",
                  "heading": dataInsightsTask.heading,
                  "description": dataInsightsTask.description,
                  "id": dataInsightsTask.id
                }) }}
                {% endif %}
            </li>
        </ul>
      </div>
    </div>
  </div>
{% endblock %}

{% block pageScripts %}
  <script src="/assets/js/card.js"></script>
{% endblock %}
